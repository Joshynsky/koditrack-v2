import 'property_form_screen.dart';

/// Backwards-compatible alias — use [PropertyFormScreen] for create/edit.
class CreatePropertyScreen extends PropertyFormScreen {
  const CreatePropertyScreen({super.key}) : super();
}
